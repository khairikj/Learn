import 'package:flutter/material.dart';

class HistoryOwner extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This history for owner page'),
    );
  }
}
