import 'package:flutter/material.dart';

class HistoryCust extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('This history for customer page'),
    );
  }
}
